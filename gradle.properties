# Fabric / Minecraft
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.17.2
fabric_version=0.116.6+1.21.1

# Kotlin (needed because Cobblemon public APIs reference kotlin.* types)
fabric_kotlin_version=1.13.6+kotlin.2.2.20

# Loom remapping can be memory-hungry; bump Gradle heap to avoid OOM during configuration.
org.gradle.jvmargs=-Xmx4G

# Mod metadata
mod_version=0.1.0
maven_group=com.modernexpshare
archives_base_name=modern-exp-share
